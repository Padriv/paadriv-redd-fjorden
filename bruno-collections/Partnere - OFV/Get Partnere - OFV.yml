info:
  name: Get Partnere - OFV
  type: http
  seq: 1

http:
  method: GET
  url: https://api.airtable.com/v0/{{app}}/{{table}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
