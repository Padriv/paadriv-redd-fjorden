info:
  name: Delete Partnere - OFV
  type: http
  seq: 2

http:
  method: DELETE
  url: https://api.airtable.com/v0/{{app}}/{{table}}/{{record}}
  auth: inherit

runtime:
  variables:
    - name: record
      value: <må settes>

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
