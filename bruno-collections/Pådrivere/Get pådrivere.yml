info:
  name: Get pådrivere
  type: http
  seq: 3

http:
  method: GET
  url: https://api.airtable.com/v0/{{app}}/{{table}}
  auth: inherit

runtime:
  variables:
    - name: record
      value: recK6UE1PlR1m3Zw5

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
